import React from 'react';
import { brandList } from '~/assets';

const BrandList: React.FC = () => {
    return (
        <>
            <img src={brandList} alt="" />
        </>
    );
};

export default BrandList;
